map $http_upgrade $connection_upgrade { default upgrade; '' close; } // SSE, WebSocket, gRPC 대비

# ─────────────────────────────────────────────────────────
# 80: ACME 예외 + 나머지 HTTPS 리다이렉트
# ── api.hilingual.kr (운영, HTTP만)
server {
  listen 80;
  server_name api.hilingual.kr;

  include /etc/nginx/includes/prod.inc;

  # ACME 예외
  location ^~ /.well-known/acme-challenge/ {
    root /var/www/certbot;
    default_type "text/plain";
    try_files $uri =404;
  }

  # 나머지는 HTTPS로
  location / {
    return 308 https://$host$request_uri;
  }
}

# ── api-dev.hilingual.kr (HTTP만)
server {
  listen 80;
  server_name api-dev.hilingual.kr;

  include /etc/nginx/includes/dev.inc;

  # ACME 예외
  location ^~ /.well-known/acme-challenge/ {
    root /var/www/certbot;
    default_type "text/plain";
    try_files $uri =404;
  }

  # 나머지는 HTTPS로
  location / {
    return 308 https://$host$request_uri;
  }
}

# ── hilingual.kr (루트 도메인, HTTP만)
server {
  listen 80;
  server_name hilingual.kr;

  # ACME 예외
  location ^~ /.well-known/acme-challenge/ {
    root /var/www/certbot;
    default_type "text/plain";
    try_files $uri =404;
  }

  # 나머지는 HTTPS로 (메인 API 도메인으로 고정 리다이렉트)
  location / {
    return 308 https://api.hilingual.kr$request_uri;
  }
}

# ── www.hilingual.kr (www 루트, HTTP만)
server {
  listen 80;
  server_name www.hilingual.kr;

  # ACME 예외
  location ^~ /.well-known/acme-challenge/ {
    root /var/www/certbot;
    default_type "text/plain";
    try_files $uri =404;
  }

  # 나머지는 HTTPS로 (메인 API 도메인으로 고정 리다이렉트)
  location / {
    return 308 https://api.hilingual.kr$request_uri;
  }
}

# ─────────────────────────────────────────────────────────
# 443: 실제 서비스 블록 (인증서 O)
# ── api.hilingual.kr (운영, HTTPS)
server {
  listen 443 ssl;
  http2;
  server_name api.hilingual.kr;

  ssl_certificate     /etc/letsencrypt/live/api.hilingual.kr/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/api.hilingual.kr/privkey.pem;

  ssl_session_timeout 1d;
  ssl_session_cache shared:MozSSL:10m;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_ciphers HIGH:!aNULL:!MD5;

  # 업스트림은 prod.inc에서 set $service_url http://IP:PORT; 로 주입
  include /etc/nginx/includes/prod.inc;

  location / {
    proxy_pass $service_url;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    proxy_set_header Authorization     $http_authorization;
    proxy_set_header Upgrade           $http_upgrade;
    proxy_set_header Connection        $connection_upgrade;

    proxy_read_timeout 60s;
    proxy_send_timeout 60s;
  }

  # 헬스체크
  location = /healthz {
    proxy_pass $service_url/actuator/health;
    proxy_set_header Host      $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_intercept_errors on;
  }

  # 모니터링 프록시 유지
  location = /grafana { return 302 /grafana/; }
  rewrite ^/prometheus$ /prometheus/ permanent;
  rewrite ^/loki$ /loki/ permanent;

  location /grafana/ {
    proxy_pass http://10.0.2.54:3000/grafana/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Prefix /grafana;
    proxy_redirect off;
  }
  location /prometheus/ {
    proxy_pass http://10.0.2.54:9090/prometheus/;
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
  location /loki/ {
    proxy_pass http://10.0.2.54:3100/;
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}

# ── api-dev.hilingual.kr (개발, HTTPS)
server {
  listen 443 ssl;
  http2;
  server_name api-dev.hilingual.kr;

  ssl_certificate     /etc/letsencrypt/live/api.hilingual.kr/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/api.hilingual.kr/privkey.pem;

  ssl_session_timeout 1d;
  ssl_session_cache shared:MozSSL:10m;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_ciphers HIGH:!aNULL:!MD5;

  # 업스트림은 dev.inc에서 set $service_url http://IP:PORT; 로 주입
  include /etc/nginx/includes/dev.inc;

  # SSE 전용 설정
  # 버퍼링을 끄지 않으면 nginx가 이벤트를 모아뒀다가 한꺼번에 보내서 실시간이 안 된다.
  # 타임아웃은 SseEmitter의 30분 타임아웃과 맞춰서 설정한다.
  location /api/v1/sse/ {
    proxy_pass $service_url;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Authorization     $http_authorization;

    proxy_buffering off;       # 버퍼링 OFF → 이벤트 즉시 클라이언트에 전달
    proxy_cache off;           # 캐시 OFF
    proxy_read_timeout 1800s;  # 30분 (SseEmitter 타임아웃과 맞춤)
    proxy_send_timeout 1800s;
    add_header X-Accel-Buffering no;  # nginx 내부 버퍼링도 OFF
  }

  location / {
    proxy_pass $service_url;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

     proxy_set_header Authorization     $http_authorization;
     proxy_set_header Upgrade           $http_upgrade;
     proxy_set_header Connection        $connection_upgrade;

     proxy_read_timeout 60s;
     proxy_send_timeout 60s;
  }

  location = /healthz {
    proxy_pass $service_url/actuator/health;
    proxy_set_header Host      $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_intercept_errors on;
  }

  # (원하면) 개발에서도 모니터링 프록시 동일 적용
  location = /grafana { return 302 /grafana/; }
  rewrite ^/prometheus$ /prometheus/ permanent;
  rewrite ^/loki$ /loki/ permanent;

  location /grafana/ {
    proxy_pass http://10.0.2.54:3000/grafana/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Prefix /grafana;
    proxy_redirect off;
  }
  location /prometheus/ {
    proxy_pass http://10.0.2.54:9090/prometheus/;
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
  location /loki/ {
    proxy_pass http://10.0.2.54:3100/;
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}

# ── hilingual.kr (루트, HTTPS: 메인 API로 고정 리다이렉트)
server {
  listen 443 ssl;
  http2;
  server_name hilingual.kr;

  ssl_certificate     /etc/letsencrypt/live/api.hilingual.kr/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/api.hilingual.kr/privkey.pem;

  return 308 https://api.hilingual.kr$request_uri;
}

# ── www.hilingual.kr (www 루트, HTTPS: 메인 API로 고정 리다이렉트)
server {
  listen 443 ssl;
  http2;
  server_name www.hilingual.kr;

  ssl_certificate     /etc/letsencrypt/live/api.hilingual.kr/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/api.hilingual.kr/privkey.pem;

  return 308 https://api.hilingual.kr$request_uri;
}
